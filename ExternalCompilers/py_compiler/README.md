# py_compiler: A Jinja2 based compiler for KerboLisp JSON

A python compiler for producing other language output from the KOSLisp Json format. The
input is compatible with the KOSLisp compiler and can be execued as a subcompiler, the
output is controlled by the choice of templates used.
